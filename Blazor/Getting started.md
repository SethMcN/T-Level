#Blazor

The -o|--output option creates a folder for the project and names the project BlazorSample:

```
dotnet new blazor -o BlazorSample
```


Change the directory to the BlazorSample folder with the following command:
```
cd BlazorSample
```


The dotnet watch command runs the app and opens your default browser to the app's landing page:

```
dotnet watch
```

Docs: https://learn.microsoft.com/en-gb/aspnet/core/get-started?view=aspnetcore-10.0
